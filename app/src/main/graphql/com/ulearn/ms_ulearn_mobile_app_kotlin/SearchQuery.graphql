query SearchQuery($id: String!) {
    SearchQuestion(id: $id) {
        statement
        score
        answers {
            context
            is_correct
        }
    }
}

query SearchQuestions {
    SearchQuestions {
        statement
        score
        answers {
            context
            is_correct
        }
    }
}