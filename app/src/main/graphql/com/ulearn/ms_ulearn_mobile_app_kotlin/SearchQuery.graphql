query SearchQuery($id: String!) {
    SearchQuestion(id: $id) {
        id
        statement
        score
        answers {
            context
            is_correct
        }
    }
}

query SearchQuestions {
    SearchQuestions {
        id
        statement
        score
        answers {
            context
            is_correct
        }
    }
}